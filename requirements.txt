# Python dependencies for Streamlit Cloud deployment
# Generated from backend/pyproject.toml — FastAPI/uvicorn excluded (not needed for Streamlit)

streamlit==1.41.1
anthropic==0.52.2
httpx==0.27.2
httpcore==1.0.7
pydantic==2.10.6
pillow==11.1.0
imagehash==4.3.2

# ffmpeg-python is NOT needed — we call the ffmpeg binary directly (via packages.txt)
