# Python dependencies for Streamlit Cloud deployment
# Loose pins so the resolver can pick wheels matching the runtime Python.
# FastAPI/uvicorn excluded — not needed for the Streamlit entry point.

streamlit>=1.41
anthropic>=0.52
pydantic>=2.10
pillow>=11.1
imagehash>=4.3
